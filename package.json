{
  "name": "shalat-ng",
  "description": "Praytimes (shalat)",
  "version": "0.0.0",
  "engines": {
    "node": "0.12.0"
  },
  "private": true,
  "repository": "http://bitbucket.org/mineral/shalat-ng",
  "devDependencies": {
    "http-server": "^0.6.1"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1",
    "pretest": "npm install"
  },
  "dependencies": {
    "bower": "^1.3.1",
    "express": "^4.13.3"
  }
}
